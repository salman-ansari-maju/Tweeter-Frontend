export type TUser = {
  id: number;
  name: string;
  isFollow: boolean;
};

export type TPost = {
  id: number;
  content: string;
};

export type TUserf = {
  id: number;
  name: string;
  isFollow: boolean;
  fun: void;
};
